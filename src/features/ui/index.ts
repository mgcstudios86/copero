/**
 * Compat shim — barrel legacy para callers del compass (MGC-321 C1).
 * Redirige a los componentes del design system (MGC-297).
 */
export { Button } from './Button';
export { Card } from './Card';
export { ScoreBadge } from './ScoreBadge';
export { Timer } from './Timer';
